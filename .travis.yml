language: python
python:
  - "2.7"
  - "3.4"

cache: pip

addons:
  apt:
    packages:
    - python-pip
    - python-numpy
    - python-scipy

install:
  - pip install matplotlib
  - python setup.py install

script: nosetests -w tests

after_success:
  - coveralls